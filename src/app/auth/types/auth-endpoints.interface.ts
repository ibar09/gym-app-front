import { constants } from "src/app/common/constants";

export const authEndpoints ={
    login:constants.url+'user/login',
    userRegister:constants.url+'user/register',
    coachRegister:constants.url+'coach/register',

}