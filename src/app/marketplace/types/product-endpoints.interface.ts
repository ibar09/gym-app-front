import { constants } from "src/app/common/constants";

export const productEndpoints ={
    findAll:constants.url+'product/list',
    add:constants.url+'product/',
    update:constants.url+'product/',
    delete:constants.url+'product/',
    findById:constants.url+'product/',
}